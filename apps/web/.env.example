# Optional: full API origin when not using the Vite dev proxy (e.g. `pnpm preview`).
# Leave unset during `pnpm dev` — requests use `/api` and Vite proxies to localhost:3001.
# VITE_API_BASE_URL=http://localhost:3001
