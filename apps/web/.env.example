# API server origin (no path). Web calls http://localhost:3001/api/... with CORS + cookies.
VITE_API_ORIGIN=http://localhost:3001

# Legacy alias (same as VITE_API_ORIGIN if set without /api suffix)
# VITE_API_BASE_URL=http://localhost:3001
